<%- include('../partials/header')-%>
<%- include('../partials/navbar')-%>


<div class="container">
  <div class="row mt-3 mb-5">
      <div class="col-md-12 mt-5">
         <h3><%=cours.title%></h3>
         <h6><%=cours.level%></h6>
         <hr>
         <p><%= cours.content%></p>
         <a href="<%= '/cours/edit/' + cours.id %>" class="btn main-btn">Edit</a>
         <button class="btn main-btn" onclick="deleteCours()" id="deleteBtn" data-id="<%= cours.id%>">Delete</button>
      </div>
  </div>
</div>

<%- include('../partials/footer')-%>